# LocalDevine User Guide

คู่มือการใช้งาน LocalDevine ฉบับสมบูรณ์

---

## 📖 สารบัญ

1. [เริ่มต้นใช้งาน](#-เริ่มต้นใช้งาน)
2. [การจัดการ Services](#-การจัดการ-services)
3. [การสร้างโปรเจค](#-การสร้างโปรเจค)
4. [Virtual Hosts](#-virtual-hosts)
5. [Database Management](#-database-management)
6. [Hosts File Editor](#-hosts-file-editor)
7. [Settings](#-settings)
8. [Troubleshooting](#-troubleshooting)

---

## 🚀 เริ่มต้นใช้งาน

### ขั้นตอนแรก

1. **เปิด LocalDevine**
   - ดับเบิลคลิก `LocalDevine.exe`
   - แนะนำ: คลิกขวา → **Run as Administrator** (สำหรับ Virtual Hosts)

2. **Start Services**
   - คลิกปุ่ม **Start** บน **Apache**
   - คลิกปุ่ม **Start** บน **MariaDB**
   - รอสักครู่จนสถานะเป็น **Running** (สีเขียว)

3. **ทดสอบ**
   - เปิด Browser ไปที่ `http://localhost`
   - ควรเห็นหน้า Welcome หรือ Index

### หน้าหลัก (Dashboard)

```
┌─────────────────────────────────────────────────┐
│  LocalDevine                                    │
├─────────────────────────────────────────────────┤
│  [📁 www] [📄 Config] [💻 Terminal] [🗄️ Database] │
├─────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │  Apache  │ │   PHP    │ │ MariaDB  │        │
│  │  [Start] │ │  Active  │ │  [Start] │        │
│  └──────────┘ └──────────┘ └──────────┘        │
├─────────────────────────────────────────────────┤
│  Console Logs                                   │
│  > Apache started on port 80                    │
│  > MariaDB started on port 3306                 │
└─────────────────────────────────────────────────┘
```

---

## ⚙️ การจัดการ Services

### Apache Web Server

**Start Apache:**
1. คลิกปุ่ม **Start** บน Apache card
2. รอจนสถานะเป็น **Running**
3. เปิด `http://localhost` เพื่อทดสอบ

**Stop Apache:**
1. คลิกปุ่ม **Stop** บน Apache card
2. รอจนสถานะเป็น **Stopped**

**Restart Apache:**
- คลิก **Stop** แล้ว **Start** ใหม่
- จำเป็นหลังแก้ไข config หรือเพิ่ม Virtual Host

### MariaDB Database

**Start MariaDB:**
1. คลิกปุ่ม **Start** บน MariaDB card
2. รอจนสถานะเป็น **Running**

**Stop MariaDB:**
1. คลิกปุ่ม **Stop** บน MariaDB card
2. รอจนสถานะเป็น **Stopped**

### PHP

PHP จะทำงานอัตโนมัติร่วมกับ Apache ไม่ต้อง start แยก

---

## 📁 การสร้างโปรเจค

### สร้างโปรเจคใหม่

1. ไปที่เมนู **Projects**
2. คลิก **Create New Project**
3. กรอกข้อมูล:
   - **Project Name:** ชื่อโปรเจค (ใช้เป็น URL path)
   - **Template:** เลือก PHP Basic หรือ HTML Basic
   - **Create Database:** ติ๊กถ้าต้องการสร้าง database
4. คลิก **Create**

### Templates ที่มี

| Template | รายละเอียด |
|----------|------------|
| **PHP Basic** | โปรเจค PHP พร้อม index.php |
| **HTML Basic** | โปรเจค HTML/CSS/JS พื้นฐาน |

### เข้าถึงโปรเจค

- **URL:** `http://localhost/project-name`
- **Folder:** `www/project-name`

### ลบโปรเจค

1. ไปที่เมนู **Projects**
2. คลิกไอคอน **🗑️** ของโปรเจคที่ต้องการลบ
3. ยืนยันการลบ

> ⚠️ **คำเตือน:** การลบโปรเจคจะลบไฟล์ทั้งหมดในโฟลเดอร์

---

## 🌐 Virtual Hosts

Virtual Hosts ช่วยให้คุณใช้ domain ท้องถิ่นแทน `localhost/project`

### ข้อดีของ Virtual Hosts

| แบบเดิม | แบบ Virtual Host |
|---------|------------------|
| `http://localhost/mysite` | `http://mysite.local` |
| `http://localhost/blog` | `http://blog.test` |

### สร้าง Virtual Host

1. **รัน LocalDevine เป็น Administrator** (สำคัญมาก!)
2. ไปที่เมนู **Virtual Hosts**
3. คลิก **Add Virtual Host**
4. กรอกข้อมูล:
   - **Name:** ชื่อ (สำหรับแสดงใน UI)
   - **Domain:** เช่น `mysite.local`
   - **Path:** เลือก folder ของโปรเจค
5. คลิก **Add**
6. **Restart Apache**

### Domain ที่แนะนำ

| Domain | หมายเหตุ |
|--------|----------|
| `*.local` | แนะนำ |
| `*.test` | แนะนำ |
| `*.dev` | ไม่แนะนำ (ใช้โดย Google) |
| `*.localhost` | ใช้ได้ |

### ลบ Virtual Host

1. ไปที่เมนู **Virtual Hosts**
2. คลิกไอคอน **🗑️** ของ host ที่ต้องการลบ
3. Restart Apache

---

## 🗄️ Database Management

### เข้าถึง Adminer

1. คลิกปุ่ม **🗄️ Database** ที่หน้าหลัก
2. Adminer จะเปิดใน Browser
3. Login (Local Database):
   - **System:** MySQL
   - **Server:** 127.0.0.1
   - **Username:** root
   - **Password:** root

### เชื่อมต่อ Remote Database (Server บน Internet)

Adminer รองรับการเชื่อมต่อฐานข้อมูลบนเซิร์ฟเวอร์ภายนอก:

1. เปิด Adminer (คลิกปุ่ม **🗄️ Database**)
2. ใส่ข้อมูลการเชื่อมต่อ:
   - **System:** MySQL / MariaDB
   - **Server:** `IP_ADDRESS:PORT` หรือ `hostname:PORT` (เช่น `example.com:3306` หรือ `192.168.1.100:3306`)
   - **Username:** username ของ database
   - **Password:** password ของ database
   - **Database:** (ชื่อ database - ว่างได้ถ้าต้องการเลือกทีหลัง)
3. คลิก **Login**

**หมายเหตุ:** ต้องแน่ใจว่าเซิร์ฟเวอร์อนุญาตการเชื่อมต่อจากภายนอก และ Firewall เปิด port MySQL/MariaDB (โดยปกติ 3306)

### สร้าง Database ใหม่

1. เข้า Adminer
2. คลิก **Create database**
3. ใส่ชื่อ database
4. เลือก **utf8mb4_unicode_ci**
5. คลิก **Save**

### Import Database (รองรับไฟล์ขนาดใหญ่)

LocalDevine รองรับการ import ไฟล์ SQL ขนาดใหญ่ (สูงสุด 512MB) - แก้ปัญหาที่ phpMyAdmin อัปโหลดไม่ได้:

1. เข้า Adminer
2. เลือก database
3. คลิก **Import**
4. เลือกไฟล์ `.sql` (รองรับไฟล์ใหญ่ถึง **512MB**)
5. คลิก **Execute**

**ข้อดีของ Adminer เทียบกับ phpMyAdmin:**
- ✅ รองรับไฟล์ใหญ่ได้ดีกว่า (streaming file upload)
- ✅ ใช้หน่วยความจำน้อยกว่า
- ✅ เร็วและเบากว่า
- ✅ Single file - ติดตั้งง่าย

**คำแนะนำ:** สำหรับไฟล์ที่ใหญ่มาก (> 512MB) ยังแนะนำให้ใช้ CLI (mysql command) สำหรับประสิทธิภาพสูงสุด

### Export Database

1. เข้า Adminer
2. เลือก database
3. คลิก **Export**
4. เลือก format (SQL)
5. คลิก **Export**

---

## 📝 Hosts File Editor

แก้ไข Windows hosts file โดยไม่ต้องเปิด Notepad

### เข้าถึง Hosts Editor

1. ไปที่เมนู **Hosts File**
2. ดูรายการ entries ทั้งหมด

### เพิ่ม Entry

1. คลิก **Add Entry**
2. กรอก IP Address (เช่น `127.0.0.1`)
3. กรอก Hostname (เช่น `mysite.local`)
4. คลิก **Add**

### ลบ Entry

1. คลิกไอคอน **🗑️** ของ entry
2. ยืนยันการลบ

> ⚠️ **หมายเหตุ:** ต้องรัน LocalDevine เป็น Administrator

---

## ⚙️ Settings

### เปลี่ยน Ports

1. ไปที่เมนู **Settings**
2. แก้ไข port ที่ต้องการ:
   - **Apache Port:** default 80
   - **MariaDB Port:** default 3306
   - **PHP Port:** default 9000
3. คลิก **Save**
4. Restart services

### Auto-start Services

1. ไปที่เมนู **Settings**
2. เปิด **Auto-start services on launch**
3. Services จะ start อัตโนมัติเมื่อเปิด app

### แก้ไข PHP Configuration (php.ini)

**ตำแหน่งไฟล์ php.ini:**
- `C:\LocalDevine\config\php.ini` (default)
- หรือตาม path ที่คุณกำหนดใน Settings → Data Path

**วิธีแก้ไข:**
1. เปิดไฟล์ `C:\LocalDevine\config\php.ini` ด้วย Notepad หรือ text editor อื่น
2. แก้ไขค่าที่ต้องการ (เช่น `upload_max_filesize`, `memory_limit`, `max_execution_time`)
3. **Restart PHP service** เพื่อให้การเปลี่ยนแปลงมีผล

**ตัวอย่างการแก้ไข:**
```ini
; เพิ่มขนาดไฟล์ที่อัปโหลดได้
upload_max_filesize = 512M
post_max_size = 1024M

; เพิ่มหน่วยความจำ
memory_limit = 512M

; เพิ่มเวลาในการ execute
max_execution_time = 300
```

**หมายเหตุ:**
- ไฟล์ `php.ini` จะถูกสร้างอัตโนมัติเมื่อรัน LocalDevine ครั้งแรก
- การแก้ไขไฟล์ใน `config/php.ini` จะไม่ถูก overwrite ตอนอัปเดตโปรแกรม
- ไฟล์ `php.ini` เดิมอยู่ใน `app.asar.unpacked/bin/php/php.ini` (ไม่แนะนำให้แก้ไข)

### แก้ไข Apache Configuration (httpd.conf)

**ตำแหน่งไฟล์:**
- `C:\LocalDevine\config\httpd.conf` (default)

**วิธีแก้ไข:**
1. ไปที่ **Settings** → **Configuration Files**
2. คลิก **httpd.conf** เพื่อเปิดไฟล์ด้วย text editor
3. แก้ไขการตั้งค่า Apache ตามต้องการ
4. **Restart Apache** เพื่อให้การเปลี่ยนแปลงมีผล

**หมายเหตุ:**
- ไฟล์ `httpd.conf` ถูกสร้างและอัปเดตอัตโนมัติเมื่อมีการเปลี่ยนแปลง Virtual Hosts
- การแก้ไขด้วยตนเองจะไม่ถูก overwrite เมื่อเพิ่ม/ลบ Virtual Host (แต่จะ merge)

### Configuration Files ทั้งหมด

ไฟล์ configuration ทั้งหมดอยู่ใน `C:\LocalDevine\config\`:

| ไฟล์ | คำอธิบาย | วิธีแก้ไข |
|------|----------|-----------|
| `php.ini` | PHP Configuration | Settings → Config Files → php.ini |
| `httpd.conf` | Apache Configuration | Settings → Config Files → httpd.conf |
| `config.json` | App Settings (ports, vhosts) | Settings → Config Files → config.json |
| `my.ini` | MariaDB Configuration | Settings → Config Files → my.ini |

**Quick Access:**
- ไปที่ **Settings** → **Configuration Files**
- คลิกที่ไฟล์ที่ต้องการแก้ไข จะเปิดด้วย default text editor ทันที

**Auto Backup:**
- ไฟล์ config ทั้งหมดจะถูก backup อัตโนมัติก่อนเปิดแก้ไข
- Backups เก็บไว้ที่ `C:\LocalDevine\config\backups\`
- เก็บ backup ไว้ล่าสุด 5 เวอร์ชันต่อไฟล์
- ถ้าแก้ config แล้ว service ไม่ start สามารถ restore กลับได้

### แก้ไข MariaDB Configuration (my.ini)

**ตำแหน่งไฟล์:**
- `C:\LocalDevine\config\my.ini` (default)

**วิธีแก้ไข:**
1. ไปที่ **Settings** → **Configuration Files**
2. คลิก **my.ini** เพื่อเปิดไฟล์ด้วย text editor
3. แก้ไขการตั้งค่า MariaDB ตามต้องการ (เช่น `max_connections`, `query_cache_size`)
4. **Restart MariaDB** เพื่อให้การเปลี่ยนแปลงมีผล

**หมายเหตุ:**
- ไฟล์ `my.ini` template จะถูกสร้างอัตโนมัติเมื่อรัน LocalDevine ครั้งแรก
- ถ้าไม่มี `my.ini` MariaDB จะใช้ command line arguments แทน

---

## 🔧 Troubleshooting

### Apache ไม่ start

**สาเหตุที่เป็นไปได้:**
1. Port 80 ถูกใช้งานโดยโปรแกรมอื่น (Skype, IIS, etc.)
2. Config file มีข้อผิดพลาด

**วิธีแก้:**
```bash
# ตรวจสอบ port 80
netstat -ano | findstr :80

# หยุดโปรแกรมที่ใช้ port 80
# หรือเปลี่ยน port ใน Settings
```

### MariaDB ไม่ start

**สาเหตุที่เป็นไปได้:**
1. Port 3306 ถูกใช้งาน
2. มีไฟล์ lock ค้างอยู่

**วิธีแก้:**
1. ลบไฟล์ `bin/mariadb/data/*.pid`
2. ลองใหม่

### Virtual Host ไม่ทำงาน

**Checklist:**
- [ ] รัน LocalDevine เป็น Administrator
- [ ] Domain อยู่ใน hosts file
- [ ] Restart Apache หลังเพิ่ม Virtual Host
- [ ] ตรวจสอบ path ถูกต้อง

### เข้า localhost ไม่ได้

**Checklist:**
- [ ] Apache กำลัง running
- [ ] ไม่มี firewall block
- [ ] ลอง `http://127.0.0.1` แทน

### Adminer login ไม่ได้

**ตรวจสอบ:**
- MariaDB กำลัง running
- ใช้ credentials:
  - Server: `127.0.0.1`
  - Username: `root`
  - Password: `root`

### Restore Config File จาก Backup

ถ้าแก้ config แล้ว service ไม่ start สามารถ restore กลับได้:

1. ไปที่ **Settings** → **Configuration Files**
2. ตรวจสอบ log หรือ error message เพื่อดูว่าไฟล์ไหนมีปัญหา
3. เปิด **Config Folder** → `backups` folder
4. หา backup file ที่ต้องการ (นามสกุล `.backup`)
5. Copy ไฟล์ backup ไปแทนที่ config file เดิม

**ตัวอย่าง:**
- Config file: `C:\LocalDevine\config\php.ini`
- Backup file: `C:\LocalDevine\config\backups\php.1705123456.ini.backup`
- Copy backup file และเปลี่ยนชื่อเป็น `php.ini` เพื่อ restore

---

## 📞 ติดต่อสอบถาม

- **GitHub Issues:** [Report Bug](https://github.com/ohmiler/localdevine/issues)
- **Email:** ติดต่อผ่าน GitHub

---

<p align="center">
  <strong>LocalDevine</strong> - Modern Local Development Environment
</p>
